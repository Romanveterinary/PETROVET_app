name: Debug Files
on: [workflow_dispatch, push]
jobs:
  list-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show all files
        run: find . -maxdepth 3 -not -path '*/.*'
